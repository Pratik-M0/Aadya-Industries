import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import { services } from '../../../lib/data'; import { DetailBody,DetailHero } from '../../../components/sections'; import { FAQ } from '../../../components/interactive';
export function generateStaticParams(){return services.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=services.find(x=>x.slug===slug);return {title:p?.title??'Service',description:p?.summary}}
export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=services.find(x=>x.slug===slug);if(!p)return notFound();return <main><DetailHero item={p} kind="Service"/><DetailBody item={p} kind="Service"/><section className="section bg-mist"><div className="container max-w-3xl"><h2 className="text-3xl font-extrabold text-navy">Service FAQs</h2><FAQ/></div></section></main>}
