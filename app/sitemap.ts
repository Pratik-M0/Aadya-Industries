import type { MetadataRoute } from 'next'; import { products,services } from '../lib/data'; export default function sitemap():MetadataRoute.Sitemap{const base='https://www.aadyaindustries.example';return ['','/about','/products','/services','/industries','/projects','/contact',...products.map(p=>`/products/${p.slug}`),...services.map(s=>`/services/${s.slug}`)].map(url=>({url:base+url,lastModified:new Date(),changeFrequency:'monthly',priority:url===''?1:.7}))}
