import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import { products } from '../../../lib/data'; import { DetailBody,DetailHero } from '../../../components/sections'; import { FAQ } from '../../../components/interactive';
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=products.find(x=>x.slug===slug);return {title:p?.title??'Product',description:p?.summary}}
export default async function Product({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)return notFound();return <main><DetailHero item={p} kind="Product"/><DetailBody item={p} kind="Product"/><section className="section bg-mist"><div className="container grid gap-8 lg:grid-cols-2"><div><h2 className="text-3xl font-extrabold text-navy">Technical specifications</h2><p className="mt-4 text-slate-600">[Add verified dimensions, material options, capacities, design conditions and documentation requirements for this product.]</p></div><div><h2 className="text-3xl font-extrabold text-navy">Product FAQs</h2><FAQ/></div></div></section></main>}
