import Link from 'next/link'; import { ArrowRight } from 'lucide-react';
export function Button({href,children,variant='primary'}:{href:string;children:React.ReactNode;variant?:'primary'|'secondary'|'dark'}){const styles={primary:'bg-orange text-white hover:bg-[#dd7000]',secondary:'border border-white/50 text-white hover:bg-white hover:text-navy',dark:'bg-navy text-white hover:bg-[#1a2d57]'};return <Link href={href} className={'focus-ring inline-flex items-center gap-2 rounded-md px-5 py-3 text-sm font-bold transition-colors '+styles[variant]}>{children}<ArrowRight size={16}/></Link>}
export function SectionTitle({eyebrow,title,copy,light=false}:{eyebrow:string;title:string;copy?:string;light?:boolean}){return <div className="max-w-2xl"><p className="eyebrow">{eyebrow}</p><h2 className={'mt-4 text-3xl font-extrabold tracking-tight md:text-4xl '+(light?'text-white':'text-navy')}>{title}</h2>{copy&&<p className={'mt-4 leading-7 '+(light?'text-slate-300':'text-slate-600')}>{copy}</p>}</div>}
